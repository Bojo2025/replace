# Web app moved to `/docs`

GitHub Pages only allows `/(root)` or `/docs` as the publish folder.

**Canonical web app:** [`../docs/`](../docs/)

Edit files under `docs/`, then commit and push.  
Pages settings: **main** branch → **`/docs`** folder → Save.

Site: https://bojo2025.github.io/replace/
